/** @type {import('next').NextConfig} */
const nextConfig = {
  // Use default output (server/static hybrid). Removing `output: "export"`
  // avoids requiring `generateStaticParams()` for dynamic routes.
}

export default nextConfig
